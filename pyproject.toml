[project]
name = "carousel-app"
version = "0.1.0"
description = "Channel-branded carousel renderer for PNG, PPTX, and video slides."
requires-python = ">=3.9"
dependencies = [
  "openai>=2.41.0",
  "pillow>=10.0.0",
  "playwright>=1.48",
  "python-pptx>=1.0.2",
  "yt-dlp>=2025.6.9",
]
