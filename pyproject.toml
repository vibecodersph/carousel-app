[project]
name = "carousel-app"
version = "0.1.0"
description = "vibecodersph branded carousel renderer for PNG and video slides."
requires-python = ">=3.9"
dependencies = [
  "openai>=2.41.0",
  "playwright>=1.48",
  "yt-dlp>=2025.6.9",
]
