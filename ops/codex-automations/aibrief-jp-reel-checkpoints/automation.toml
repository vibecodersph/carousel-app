version = 1
id = "aibrief-jp-reel-checkpoints"
kind = "cron"
name = "AI Brief JP — Reel checkpoints"
prompt = "Act as the AI Brief JP Reel checkpoint recorder in /Users/aiagent/GitHub/carousel-app. Run exactly: UV_CACHE_DIR=state/uv-cache uv run --frozen python scripts/run_aibrief_jp_reel_checkpoints.py. This task is analytics-only. Never queue, reschedule, edit, render, delete, publish, or repost content. The script may append exact-media insight snapshots to state/reels.db and write immutable Markdown checkpoint files only under out/aibrief_jp_reel_learning/. If files are recorded, report their paths and whether they are +1h, +3h, +24h, or MISSED_CHECKPOINT. If nothing is due, say so briefly. If Graph access or the scheduler lock prevents the run, report the exact error and do not fabricate a checkpoint from a later snapshot."
status = "ACTIVE"
rrule = "FREQ=DAILY;BYHOUR=0,9,10,12,13,14,16,18,19,21,22;BYMINUTE=30;BYSECOND=0"
model = "gpt-5.5"
reasoning_effort = "medium"
execution_environment = "local"
target = { type = "project", project_id = "/Users/aiagent/GitHub/carousel-app" }
cwds = ["/Users/aiagent/GitHub/carousel-app"]
created_at = 1783949939000
updated_at = 1783949939000
